class Solution 
{
public:
    string getEncryptedString(string s, int k) 
    {
       string original = s;
       int size=s.length();
       for(int i=0 ; i<s.length() ; i++)
       {
          s[i]=original[(i+k)%size];
       }    
       return s;
    }
};
